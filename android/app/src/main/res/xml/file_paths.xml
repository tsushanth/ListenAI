<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cache directory for TTS audio files -->
    <cache-path name="audio_cache" path="." />

    <!-- External cache directory (fallback) -->
    <external-cache-path name="external_audio_cache" path="." />

    <!-- Files directory for permanent storage -->
    <files-path name="audio_files" path="." />
</paths>

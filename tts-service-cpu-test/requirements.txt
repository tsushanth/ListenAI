# ReadAloud AI CPU TTS - Minimal dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3

# Kokoro ONNX - CPU-optimized inference (no PyTorch needed!)
kokoro-onnx>=0.4.0

# ONNX Runtime for CPU inference
onnxruntime>=1.17.0

# Audio processing
soundfile==0.12.1
numpy>=1.26.0

# HTTP
httpx==0.27.0

# Edge TTS fallback for languages Kokoro 82M doesn't include (de, ko).
# Microsoft's free public service; same library audexa-radio uses for
# non-EN live radio. No API key needed.
edge-tts>=6.1.0
